// VitePWA handles service worker registration via registerType: 'autoUpdate'.
// This module exists as an extension point if you need manual hooks later.
export function registerServiceWorker() {
  // no-op for now
}

